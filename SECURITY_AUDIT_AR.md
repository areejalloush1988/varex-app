# تدقيق أمان VAREX — النسخة العربية

تاريخ التدقيق: 21 أغسطس 2026

## النتيجة التنفيذية

يحتوي VAREX على أساس أمني جيد: اتصال Supabase مصادق عليه، عزل بيانات منشآت في الجداول التشغيلية عبر RLS، وظيفة حذف حساب من جهة الخادم، وتحقق حديث بكلمة المرور ورمز البريد قبل الحذف. أظهر فحص حزم التشغيل `npm audit --omit=dev` عدم وجود ثغرات معروفة في حزم الإنتاج وقت التدقيق.

لا يجوز وصف أي نظام بأنه غير قابل للاختراق. الصياغة المعتمدة في الواجهة هي: **حماية متقدمة متعددة الطبقات مصممة لحماية بيانات المنشأة وتقليل مخاطر الوصول غير المصرح به.**

## نقاط تم التحقق منها

- مفتاح Supabase الموجود في الواجهة مفتاح نشر، ولا يوجد مفتاح `service_role` داخل ملفات العميل.
- الجداول التشغيلية الجديدة مفعّل عليها RLS وسياسات نطاق المنشأة.
- وظائف العمليات الحساسة تستخدم جلسة المستخدم وتحدد المنشأة الحالية.
- حذف حساب المنشأة لا يستخدم مفتاح الإدارة في المتصفح، ويتطلب تحققاً حديثاً بكلمة المرور ورمز البريد.
- وظيفة الحذف تقيد CORS للدومينات المعتمدة وتعيد استجابات `no-store`.
- توجد إعدادات للقفل التلقائي وسجل النشاط والصلاحيات.
- أضيفت مراقبة أسبوعية لتحديثات حزم npm وGitHub Actions.

## تحسينات طُبقت في هذه المرحلة

- إضافة صفحة «مركز الأمان» مع فحص مباشر للاتصال والجلسة وعزل المنشأة والقفل والتطبيق.
- جعل تسجيل الدخول يحترم خيار «تذكرني» بدلاً من حفظ الرمز دائماً.
- الإبقاء على الجلسة المؤقتة داخل `sessionStorage` عندما لا يختار المستخدم «تذكرني».
- تقييد أنواع وأحجام ملفات وثائق الموظفين والتحقق من بصمة الملف قبل تخزينه محلياً.
- إضافة واجهة عربية لمساعد VAREX الذكي، مع مسار خادم يحافظ على سرية مفتاح OpenAI.
- عدم إرسال بيانات المنشأة للمساعد تلقائياً وعدم حفظ المحادثة في قاعدة VAREX في هذه المرحلة.

## أعمال المرحلة التالية

1. تفعيل MFA للمالك والمدير وربطه بمستوى جلسة أعلى للعمليات الحساسة.
2. تطبيق سياسة رؤوس أمان على الدومين من Cloudflare، بما يشمل HSTS وCSP وقيود الإطارات.
3. فحص الملفات سحابياً قبل الحفظ بواسطة خدمة مكافحة برمجيات ضارة، مع حجر الملف حتى اكتمال الفحص.
4. تفعيل نسخ احتياطية دورية واختبار الاسترجاع وتوثيق زمن الاحتفاظ.
5. إضافة تحديد طلبات موزع لتسجيل الدخول وواجهة الذكاء الاصطناعي بدلاً من الاعتماد على ذاكرة نسخة الخادم فقط.
6. اختبار اختراق دفاعي قبل الإطلاق التجاري، مع مراجعة جميع مواضع HTML الديناميكي.

## ملاحظة تشغيلية

صفحة مركز الأمان تعرض حالة حقيقية لما يمكن فحصه من الجهاز والجلسة. البنود التي تحتاج إعداد خادم تظهر بوضوح على أنها «المرحلة التالية» ولا تُعرض كمزايا مفعّلة قبل تنفيذها.
